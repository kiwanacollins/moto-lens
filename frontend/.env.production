# MOTO LENS - Frontend Environment Variables (Production)
# For Vercel deployment - backend hosted on VPS

# Backend API URL - Using Vercel proxy to avoid mixed content errors
# This calls /api/proxy/* which forwards to the VPS backend
VITE_API_BASE_URL=/api/proxy

# Note: The proxy is a Vercel serverless function at /api/proxy.js
# It forwards requests to: http://207.180.249.87/api
